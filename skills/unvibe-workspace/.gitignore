# Skill-creator eval workspace.
# Transient artifacts from `iteration-N` runs and snapshots are
# regenerable and intentionally not tracked. Only the eval helper
# scripts (and this .gitignore) belong in version control.

/iteration-*/
/skill-snapshot/
/skill-snapshot-iter1/
/fixtures/
/feedback.json
/review.html
